# Playbooks — axantiq (ax-website)

Repeated procedures, exact commands. Verified-on date per playbook.

No `package.json` in this repo, so there are no `npm`/`bun`/`pnpm` scripts to list. No ship/test procedure is documented in the repo (README, CLAUDE.md, or commit history) as of 2026-07-12. This section is intentionally empty pending the next working session — do not guess a deploy command.
